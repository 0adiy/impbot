/**
 * Enum for command privacy
 * @readonly
 * @enum {string}
 */
export const CommandPrivacy = Object.freeze({
  /**
   * Private category type
   */
  PRIVATE: "private",

  /**
   * Public category type
   */
  PUBLIC: "public",
});
